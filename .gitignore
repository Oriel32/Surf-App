# Swift / SwiftPM
.build/
.swiftpm/
Package.resolved

# Xcode
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint

# Secrets — the Stormglass key never enters source control.
*.xcconfig
.env

.DS_Store

# Generated by XcodeGen from project.yml — never committed, and only creatable
# on a Mac. The macOS CI runner regenerates it on every build.
*.xcodeproj
Support/Info.plist

# Local and CI build output
build/
Payload/
*.ipa
